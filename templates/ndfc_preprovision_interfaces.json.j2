{# 
  JSON TEMPLATE To configure Interfaces in Nexus Dashboard Fabrics via the DCNM API.
  Used by playbook: 04-configure-nd-fabric-interfaces.yml
  Called via: lookup('template', '../templates/ndfc_preprovision_interfaces.json.j2')
    Purpose:
        Generate JSON payload for pre-provisioning interfaces on switches
        registered in a Nexus Dashboard Fabric.
    Input Variables:
        policy: Interface policy template name on NDFC
        interfaceType: Type of interface (e.g., INTERFACE_ETHERNET)
        interfaces: List of interface objects to configure
            Each interface object contains:
                serialNumber: Switch serial number
                ifName: Interface name (e.g., Ethernet1/1)
                nvPairs: Name-value pairs for interface settings
    Notes:
        - The 'nvPairs' field is a JSON object containing interface parameters
        - Ensure the policy template exists on NDFC before applying
#}
{
  "policy": "int_access_host",
  "interfaceType": "INTERFACE_ETHERNET",
  "interfaces": [
    {
      "serialNumber": "9B1I5QV8432",
      "ifName": "Ethernet1/20",
      "nvPairs": {
        "INTF_NAME": "Ethernet1/20",
        "BPDUGAURD_ENABLED": "true",
        "DESC": "API TEST",
        "ADMIN_STATE": "true",
        "MTU": "jumbo",
        "SPEED": "Auto",
        "ACCESS_VLAN": "199"
      }
    }
  ]
}